<%- include('../header'); %>
<body>
    <container class="jumbotron">

<h1>Your Cart!</h1>
    </container>
</br>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />

<div>
    <% items.forEach(function(item){ %>
        <ul class="img-thumbnail" onclick="onItemClick('<%-item._id %>', '<%- item.name %>', '<%- item.price %>', '<%- item.image %>')">
            <img src=<%= item.image %> >
            <h1><%= item.name %> </h1>
            <h3> ₹ <%=  item.price %> </h3>
            <label>quantity</label>
            <input type="number" id="quantity" min="1" value="<%- quantity[item._id].quantity %>">
            <h3>
                <%- quantity[item._id].total_price %>
            </h3>
            <button class="delete-button" onclick="onDeleteItem('<%- item._id %>', event)" id="<%- item._id %>"  >add to cart</button>
            </ul>
    <% }) %> 
</div>

<div>
    <h1>Amount : </h1> <h2><%- amount %></h2>
</div>

<form method="get" action="/home/confirm_order">
      <input type="hidden" name="amount" value="<%= amount %>" /> 
    <button>Checkout</button>
</form>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script>
    function onItemClick(itemID, name, price, image){
        
    }

</script>
</body>
</html>